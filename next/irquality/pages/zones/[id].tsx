import { Zone } from "@models/zone"

type Props = {
    item?: Zone
    errors?: string
}


export default function getZoneById({ zone }: { zone: Zone }) {
    return (
        <div>details </div>
    )
}